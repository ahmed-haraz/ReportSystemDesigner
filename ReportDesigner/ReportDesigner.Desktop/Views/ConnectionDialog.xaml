<Window x:Class="ReportDesigner.Desktop.Views.ConnectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Connection Wizard" Height="500" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Data Source Connection" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <TabControl Grid.Row="1">
            <TabItem Header="SQLite">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Database File:" Grid.Row="0"/>
                    <TextBox Grid.Row="1" x:Name="txtDbPath" Margin="0,5"/>
                    <Button Grid.Row="2" Content="Browse..." Click="BrowseButton_Click" HorizontalAlignment="Left" Padding="10,2"/>
                    <Button Grid.Row="3" Content="Test Connection" Click="TestSQLite_Click" VerticalAlignment="Top" Margin="0,10,0,0" Padding="15,5"/>
                </Grid>
            </TabItem>
            <TabItem Header="SQL Server">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Server:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtServer" Margin="0,2"/>

                    <TextBlock Text="Database:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtDatabase" Margin="0,2"/>

                    <TextBlock Text="Username:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="txtUsername" Margin="0,2"/>

                    <TextBlock Text="Password:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
                    <PasswordBox Grid.Row="3" Grid.Column="1" x:Name="txtPassword" Margin="0,2"/>

                    <CheckBox Grid.Row="4" Grid.Column="1" Content="Windows Authentication" x:Name="chkWindowsAuth" Margin="0,5"/>

                    <Button Grid.Row="5" Grid.Column="1" Content="Test Connection" Click="TestSqlServer_Click" HorizontalAlignment="Left" Padding="15,5" Margin="0,10,0,0"/>
                </Grid>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Click="OkButton_Click" Padding="20,5" Margin="0,0,10,0"/>
            <Button Content="Cancel" Click="CancelButton_Click" Padding="20,5"/>
        </StackPanel>
    </Grid>
</Window>
