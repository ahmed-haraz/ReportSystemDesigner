<UserControl x:Class="ReportDesigner.Desktop.Views.DesignSurface"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="842"
             d:DesignWidth="595">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Canvas Grid.Row="0"
                Background="#F0F0F0"
                x:Name="HorizontalRuler">
            <StackPanel Orientation="Horizontal" Margin="5,2">
                <TextBlock Text="Ruler" FontSize="10" Foreground="Gray" />
                <TextBlock Text=" | drag bands/objects from the toolbox; objects snap to grid and can be moved anywhere inside a band" FontSize="10" Foreground="Gray" />
            </StackPanel>
        </Canvas>

        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      x:Name="DesignScrollViewer">
            <Grid x:Name="PageContainer"
                  Margin="50">
                <Grid.LayoutTransform>
                    <ScaleTransform ScaleX="{Binding Zoom}"
                                    ScaleY="{Binding Zoom}" />
                </Grid.LayoutTransform>

                <Border Background="#CCCCCC"
                        Margin="5,5,0,0"
                        Width="{Binding PageWidth}"
                        Height="{Binding PageHeight}" />

                <Border Background="White"
                        BorderBrush="#999999"
                        BorderThickness="1"
                        Width="{Binding PageWidth}"
                        Height="{Binding PageHeight}"
                        x:Name="PageBorder">

                    <Canvas x:Name="DesignCanvas"
                            AllowDrop="True"
                            Width="{Binding PageWidth}"
                            Height="{Binding PageHeight}"
                            Drop="DesignCanvas_Drop"
                            DragOver="DesignCanvas_DragOver">

                        <!-- Grid Lines -->
                        <Canvas.Background>
                            <DrawingBrush TileMode="Tile"
                                          Viewport="0,0,20,20"
                                          ViewportUnits="Absolute">
                                <DrawingBrush.Drawing>
                                    <GeometryDrawing>
                                        <GeometryDrawing.Geometry>
                                            <RectangleGeometry Rect="0,0,20,20" />
                                        </GeometryDrawing.Geometry>
                                        <GeometryDrawing.Pen>
                                            <Pen Brush="#F0F0F0"
                                                 Thickness="0.5" />
                                        </GeometryDrawing.Pen>
                                    </GeometryDrawing>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Canvas.Background>

                        <!-- Bands - rendered as direct Canvas children for proper hit testing -->
                        <ItemsControl ItemsSource="{Binding Bands}"
                                      x:Name="BandsItemsControl"
                                      Width="{Binding PageWidth}"
                                      Height="{Binding PageHeight}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Canvas.Top" Value="{Binding Top}" />
                                    <Setter Property="Canvas.Left" Value="0" />
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Opacity="0.5"
                                            Height="{Binding Height}"
                                            Width="{Binding DataContext.PageWidth, 
                                                    RelativeSource={RelativeSource AncestorType=Canvas}}"
                                            BorderBrush="#0066CC"
                                            BorderThickness="1"
                                            MouseLeftButtonDown="BandBorder_MouseLeftButtonDown"
                                            Background="#E0F0FF">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}"
                                                                 Value="True">
                                                        <Setter Property="BorderBrush"
                                                                Value="Red" />
                                                        <Setter Property="BorderThickness"
                                                                Value="2" />
                                                        <Setter Property="Opacity"
                                                                Value="0.7" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Name}"
                                                   VerticalAlignment="Top"
                                                   HorizontalAlignment="Left"
                                                   Margin="5,2"
                                                   FontSize="10"
                                                   Foreground="#0066CC"
                                                   FontWeight="Bold"
                                                   IsHitTestVisible="False" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Objects in Selected Band -->
                        <ItemsControl ItemsSource="{Binding SelectedBand.Objects}"
                                      x:Name="ObjectsItemsControl"
                                      Width="{Binding PageWidth}"
                                      Height="{Binding PageHeight}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Canvas.Left" Value="{Binding Left}" />
                                    <Setter Property="Canvas.Top" Value="{Binding Top}" />
                                    <Setter Property="Panel.ZIndex" Value="{Binding ZIndex}" />
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="{Binding Width}"
                                            Height="{Binding Height}"
                                            BorderBrush="Blue"
                                            BorderThickness="1"
                                            Background="White"
                                            Cursor="SizeAll"
                                            MouseLeftButtonDown="ObjectBorder_MouseLeftButtonDown"
                                            MouseMove="ObjectBorder_MouseMove"
                                            MouseLeftButtonUp="ObjectBorder_MouseLeftButtonUp">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}"
                                                                 Value="True">
                                                        <Setter Property="BorderBrush"
                                                                Value="Red" />
                                                        <Setter Property="BorderThickness"
                                                                Value="2" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Text}"
                                                   FontFamily="{Binding TextProps.FontName}"
                                                   FontSize="{Binding TextProps.FontSize}"
                                                   Foreground="Black"
                                                   TextAlignment="Left"
                                                   TextWrapping="Wrap"
                                                   HorizontalAlignment="Stretch"
                                                   VerticalAlignment="Stretch"
                                                   Margin="2"
                                                   IsHitTestVisible="False" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Canvas>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
