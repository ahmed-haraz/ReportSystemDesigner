<UserControl x:Class="ReportDesigner.Desktop.Views.DataSourceWizard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Data Sources" FontWeight="Bold" Margin="5" Background="#E0E0E0" Padding="5"/>

        <ToolBar Grid.Row="1" Margin="2">
            <Button Content="+" ToolTip="Add Data Source" Command="{Binding AddDataSourceCommand}"/>
            <Button Content="-" ToolTip="Remove Data Source" Command="{Binding RemoveDataSourceCommand}"/>
            <Button Content="Test" ToolTip="Test Connection" Command="{Binding TestConnectionCommand}"/>
        </ToolBar>

        <ListBox Grid.Row="2" ItemsSource="{Binding DataSources}" 
                 SelectedItem="{Binding SelectedDataSource}" Margin="5">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Type}" FontSize="10" Foreground="Gray"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Margin="5">
            <TextBlock Text="Connection String:" FontWeight="SemiBold"/>
            <TextBox Text="{Binding ConnectionString, UpdateSourceTrigger=PropertyChanged}" 
                     TextWrapping="Wrap" AcceptsReturn="True" Height="60" FontSize="10"/>

            <TextBlock Text="Query:" FontWeight="SemiBold" Margin="0,5,0,0"/>
            <TextBox Text="{Binding SelectCommand, UpdateSourceTrigger=PropertyChanged}" 
                     TextWrapping="Wrap" AcceptsReturn="True" Height="60" FontSize="10"/>
        </StackPanel>
    </Grid>
</UserControl>
